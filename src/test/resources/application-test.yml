spring:
  datasource:
    url: jdbc:h2:mem:a;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  jms:
    template.receive-timeout: 5  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect  
app:
  gridSize: 3
